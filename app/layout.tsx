import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { ThemeProvider } from "next-themes";
import { Toaster } from "@/components/ui/sonner";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Brand Colors - Official Brand Color Palettes Library",
  description: "A curated library of official brand colors from top tech companies, sports clubs, fashion brands, and news agencies. Copy colors in HEX, RGB, HSL, or OKLCH formats.",
  keywords: ["brand colors", "color palette", "design", "hex colors", "brand guidelines", "color library"],
  authors: [{ name: "Brand Colors" }],
  openGraph: {
    title: "Brand Colors - Official Brand Color Palettes Library",
    description: "A curated library of official brand colors. Copy colors in multiple formats.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} font-sans antialiased`}
      >
        <ThemeProvider
          attribute="class"
          defaultTheme="system"
          enableSystem
          disableTransitionOnChange
        >
          {children}
          <Toaster />
        </ThemeProvider>
      </body>
    </html>
  );
}
